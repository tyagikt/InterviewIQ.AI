export const createOrder = (req, res) => {
  return res.status(200).json({
    message: "Payment feature temporarily disabled"
  });
};

export const verifyPayment = (req, res) => {
  return res.status(200).json({
    message: "Payment verification disabled"
  });
};